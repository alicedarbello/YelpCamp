<% layout('layouts/boilerplate') %>

<div class="row">
    <div class="col-6 offset-3">
<% const formValues = campground || formData.campground || {}; %>
      <h1 class="text-center">
    <%= campground ? "Edit" : "New" %> Campground
  </h1>
      <form id="campground-form"
  method="POST"  
  action="<%= campground ? `/campgrounds/${campground._id}?_method=PUT` : "/campgrounds/" %>" novalidate class="validated-form" enctype="multipart/form-data">
      <div class="mb-3"> 
        <label class="form-label" for="title">Title</label>
        <input class="form-control" type="text" id="title" name="campground[title]"
        value="<%= formValues.title || '' %>" required/>
        <%- include('../partials/fieldError', { fieldName: 'campground.title' }) %>
      </div>
      <div class="mb-3">
        <label class="form-label" for="location">Location</label>
        <input class="form-control" type="text" id="location" name="campground[location]"
        value="<%= formValues.location || '' %>" required/>
        <%- include('../partials/fieldError', { fieldName: 'campground.location' }) %>
      </div>
      <div class="mb-3">
      <label class="form-label" for="price">Campground Price</label>
      <div class="input-group">
        <span class="input-group-text" id="price-label">$</span>
        <input type="text" id="price" class="form-control" value="<%= formValues.price || '' %>" placeholder="0.00" aria-label="price" aria-describedby="price-label" name="campground[price]" required/>
      </div>
      <%- include('../partials/fieldError', { fieldName: 'campground.price' }) %>
      </div>
        <div class="mb-3">
        <label class="form-label" for="description">Description</label>
        <textarea class="form-control" id="description" name="campground[description]" required><%= formValues.description || '' %></textarea>
        <%- include('../partials/fieldError', { fieldName: 'campground.description' }) %>
      </div>
            <div class="mb-3">
        <div class="input-group">
          <input type="file" class="form-control" id="image" aria-describedby="inputGroupFileAddon04" aria-label="Upload" name="image" multiple>
      </div>
      </div>
      <div class="mb-3">
      <% if (campground && campground.images && campground.images.length > 0) {  %>
        <div class="row">
          <% campground.images.forEach(function(img, i) { %>
            <div class="col-4">
              <div class="form-check-inline">
              <img src="<%= img.thumbnail %>" alt="<%= img.filename %>" class="img-fluid mb-2">
                <input type="checkbox" id="image-<%=i%>" name="deleteImages[]" value="<%= img.filename  %>">
                <label for="image-<%=i%>">Delete image</label>
              </div>
            </div>
          <% }) %>
        </div>
        <% } %>
      </div>
      <div class="mb-3">
            <button class="btn btn-success"><%= campground ? "Edit" : "New" %> Campground</button>
          </form>
          <button class="btn btn-danger" href="<%= campground ? `/campgrounds/${campground._id}` : "/campgrounds/"%>">Cancel</button>
        </div>
    </div>
</div>

  
 

